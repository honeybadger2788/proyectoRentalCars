import { render, screen } from '@testing-library/react'
import Calendar from './Calendar'

test('renders a p tag', () => {
    render(<Calendar />);
    const 
    expect().toMatch('Sentite como en tu hogar');
    }
);